PoC 재실험(v4) 계획 수립

### **Step(phase)_1 LLM Reranking 백엔드 검증**

- **목표:**
    - 정해진 포맷(JSON)으로 결과도출하는가?
    - LLM이 멍청한 소리를 안 하고 정답을 골라내는가?
- **데이터:** 가짜 후보 리스트 (Mock Candidates)
- **활동:** Python 스크립트로 프롬프트 튜닝 & 테스트 케이스(Excel) 채점
- **산출물:**
    - `rerank_logic.py`
    - `test_results.xlsx` (정답률 80% 이상)

### **Step(phase)_2. 생성 경험 검증**

- **목표:** 키오스크에서 모바일로 정보가 끊김 없이 넘어가는가?
- **데이터:** Step 1에서 나온 JSON 결과를 그대로 하드코딩해서 사용.
- **활동:**
    - 키오스크 화면(HTML)에 QR 생성
    - 모바일 웹앱(HTML)에서 QR 찍고 나침반 화살표 돌아가는지 확인
- **산출물:**
    - `index.html`(키오스크 태블릿)
    - `nav.html`(모바일)
    - 동작 영상/캡처

### **Step(phase)_3. RAG 통합 검증 - 진짜 검색(Hybrid 검색-영근님 파트) 붙이기**

- **목표:** 실제 DB에서 검색된 결과가 Re-ranking을 거쳐 안내 되는가?
- **데이터:** 실제 상품 데이터 50~100개 (Vector DB + Elasticsearch)
- **활동:**
    - Mock 데이터를 걷어내고, 실제 검색 엔진 API 연결.
    - `STT` → `Search` → `Re-ranking` → `Response` 파이프라인 연결
- • **산출물:** 통합 백엔드 API 서버.

### **Step(phase)_4. 성능 평가 - RAGAS 적용**

- **목표:** 시스템이 얼마나 똑똑한지 점수로 증명할 수 있는가?
- **활동:** RAGAS 프레임워크를 돌려 객관적 지표 산출
- **산출물:** 성능 평가 보고서 (정확도, Context Precision 등)

### **Step(phase)_5. 현장 테스트**

- **목표:** 실제 환경(다이소 매장 or 강의실)에서 되는가?
- **활동:**
    - 태블릿(키오스크) 설치(인간 위치 고정)
    - 바닥에 보정용 QR 부착(임시)
    - 사람들이 써보고 "오!" 하는지 관찰.
- **산출물:** 최종 시연 데모.