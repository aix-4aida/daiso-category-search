FROM nginx:alpine

# Copy frontend-v3 static files
COPY frontend-v3/ /usr/share/nginx/html/

# Custom nginx config for SPA routing
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
